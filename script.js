const menuBtn=document.getElementById("menuBtn"),navMenu=document.getElementById("navMenu");menuBtn.addEventListener("click",e=>{const t=navMenu.classList.contains("active");navMenu.classList.toggle("active"),menuBtn.setAttribute("aria-expanded",(!t).toString()),e.stopPropagation()}),document.addEventListener("click",e=>{!navMenu.contains(e.target)&&e.target!==menuBtn&&(navMenu.classList.remove("active"),menuBtn.setAttribute("aria-expanded","false"))})